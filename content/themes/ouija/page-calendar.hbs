{{!< default}}

{{!-- This is a page template. A page outputs content just like any other post, and has all the same
    attributes by default, but you can also customise it to behave differently if you prefer. --}}

{{!-- Everything inside the #post tags pulls data from the page --}}
{{#post}}

<header class="main-header post-head {{#if image}}" style="background-image: url({{image}}){{else}}no-cover{{/if}}">
  {{> "nav-bar"}}
</header>

<main class="content" role="main">
  <article class="inner content-max-width {{post_class}}">

    <header class="post-header">
      <div class="calendar-arrows">
        <button id="prev" class="material-icons">keyboard_arrow_left</button>
        <button id="next" class="material-icons">keyboard_arrow_right</button>
      </div>
    </header>

    <section class="post-content">
      <div id="post-content" class="display-none">{{content}}</div>
    </section>

  </article>
</main>
{{/post}}
